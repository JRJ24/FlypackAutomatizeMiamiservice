export interface IInventoryTV {
  brandTV: string;
  inchs: string;
  model: string;
  quantity: number;
  client: string;
  clientCode?: string;
  lastMiamiInvoiceNumber?: string;
  isDisabled: boolean;
}
